/**
 * Shared Components — barrel export
 *
 * These components are used by BOTH CODEX and LAB modes.
 * Extract inline components here once patterns stabilize.
 *
 * Planned:
 *   AgentPanel.jsx       — Full agent roster with status, expandable
 *   AgentChat.jsx        — Luna + Eden chat interface (entity-scoped)
 *   GenerationQueue.jsx  — Task queue with polling status
 *   ProjectHeader.jsx    — Project name, slug, settings summary
 *   EdenStatus.jsx       — Eden connection + manna balance
 */

// Exports will be added as components are extracted from mode roots.
// For now, CODEX and LAB define their own inline placeholders.
